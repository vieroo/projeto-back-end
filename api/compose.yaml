name: 'raizes-postgres'
services:
  postgres:
    image: 'postgres:latest'
    container_name: 'raizes-postgres'
    environment:
      - 'POSTGRES_DB=raizes_db'
      - 'POSTGRES_PASSWORD=postgres'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432:5432'
